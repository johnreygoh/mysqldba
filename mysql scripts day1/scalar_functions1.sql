-- demo scalar functions

-- string functions
-- select 
-- 	upper(`customer name`) as CustomerName,
--     upper(`ship mode`) as ShipMode,
--     `product name`,
--     if(sales<100,100,sales) as sales
-- from orders;

